# Exemplo de configuracao AWS ECR
# Copie este arquivo para .env e preencha com seus valores

# ===== AWS Configuration =====
AWS_ACCOUNT_ID=123456789012
AWS_REGION=us-east-1

# Nome do Projeto (mesmo usado no Terraform)
PROJECT_NAME=togglemaster

# ===== ECR =====
ECR_REGISTRY=${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com

# ===== URLs das Imagens (auto-geradas) =====
# Formato: ${ECR_REGISTRY}/${PROJECT_NAME}/service-name:tag

ANALYTICS_IMAGE=${ECR_REGISTRY}/${PROJECT_NAME}/analytics-service:latest
AUTH_IMAGE=${ECR_REGISTRY}/${PROJECT_NAME}/auth-service:latest
EVALUATION_IMAGE=${ECR_REGISTRY}/${PROJECT_NAME}/evaluation-service:latest
FLAG_IMAGE=${ECR_REGISTRY}/${PROJECT_NAME}/flag-service:latest
TARGETING_IMAGE=${ECR_REGISTRY}/${PROJECT_NAME}/targeting-service:latest

# ===== GitHub Secrets Required =====
# Configure estes secrets no GitHub (Settings > Secrets and variables > Actions):
#
# AWS_ACCESS_KEY_ID
# AWS_SECRET_ACCESS_KEY
# AWS_REGION
# AWS_ACCOUNT_ID
# PROJECT_NAME
